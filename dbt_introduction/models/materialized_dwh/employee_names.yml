version: 2

models:
  - name: employee_names  # 生成するモデルの名前
    description: '{{ doc("employee_names") }}'  # ドキュメントの参照
    columns:
      - name: employee_id
        description: "Employee ID"
        tests:
          - unique
          - not_null
          - my_not_null  # カスタムテストの呼び出し
          # - accepted_values:
          #   values: ['hoge', 'fuga', 'piyo']
          # - relationships:
          #   to: ref('other_model_name')
          #   field: id
